#include "../includes/minishell.h"

int parse_export(char *prompt)
{
	int     i;
	char    *ptr;

	i = 0;
	ptr = ft_strnstr(prompt, "export", sizeof(prompt));
	if (!ptr)
		return (0);
	else if ()
	{
		while ()
	}
}